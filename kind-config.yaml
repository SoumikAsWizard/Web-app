# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # inside kind
        hostPort: 30080        # exposed on localhost
        protocol: TCP
      - containerPort: 30911
        hostPort: 30911
        protocol: TCP
